<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All events</title>
</head>
<style>
    body {
        margin: 0;
        background-color: #272727;
        font-family: 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        text-align: center;
    }
    .title {
        color: #15c515;
        margin-top: 20px;
        font-size: 40px;
    }
    .container{
        padding: 5px;
        display: inline-block;
        min-width: 60%;
        max-width: 80%;
        border-radius: 5px;
    }
    .main{
        background-color: #3a3a3a86;
        margin: 10px 0;
        padding: 20px 15px;
        border-radius: 5px;
        padding-bottom: 30px;
    }
    .name{
        font-size: 20px;
        text-align: left;
        margin-left: 10px;
        color: #ccc
    }
    .private{
        margin: 0 5px;
        color: #089b08;
        font-size: 12px;
        font-style: italic;
    }
    .deadline{
        text-align: left;
        color: #888888;
        float: right;
    }
    .description{
        text-align: left;
        color: #777777;
    }
    .done{
        border: none;
        border-radius: 4px;
        background-color: #26b926b2;
        color: white;
        float: right;
        padding: 7px;
        cursor: pointer;
        margin-top: 5px;
    }
    a{
        text-decoration: none;
        color: white;
    }
</style>
<body>
    <div class="title">Event Remainder</div>
    <div class="container">
        <% events.forEach(event => { %>
            <div class="main">
                <div class="name"><%= event.name %><span class="private"><%- (!event.isPublic) ? "Private" : '' %></span><a href="/event/<%= name %>/<%= rollno %>/delete/<%= event._id %>"><button class="done">Mark as done</button></a></div>
                <div class="description"><%= event.description %></div>
                <% if(event.deadline){ %><div class="deadline">Deadline: <%= event.deadline %></div><% } %>
            </div>
        <% }) %>
    </div>
</body>
</html>